/** HTTP presentation contract consumed by other API feature modules. */
export {
  AccessTokenGuard,
  type AuthenticatedRequest,
} from "@/modules/auth/presentation/access-token.guard";
export { Roles, RolesGuard } from "@/modules/auth/presentation/roles";
